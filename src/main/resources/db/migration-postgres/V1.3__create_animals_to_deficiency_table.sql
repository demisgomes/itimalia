CREATE TABLE IF NOT EXISTS animal_to_deficiency (id SERIAL PRIMARY KEY, animal_id INT NOT NULL, animal_deficiency_id INT NOT NULL, CONSTRAINT fk_animal_to_deficiency_animal_id_id FOREIGN KEY (animal_id) REFERENCES animals(id) ON DELETE CASCADE ON UPDATE RESTRICT, CONSTRAINT fk_animal_to_deficiency_animal_deficiency_id_id FOREIGN KEY (animal_deficiency_id) REFERENCES animal_deficiency(id) ON DELETE RESTRICT ON UPDATE RESTRICT)