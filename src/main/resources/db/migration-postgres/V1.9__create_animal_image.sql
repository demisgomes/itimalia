CREATE TABLE IF NOT EXISTS animal_images (id SERIAL PRIMARY KEY, "name" VARCHAR(255) NOT NULL, path VARCHAR(255) NULL, format VARCHAR(40) NOT NULL, bytes INT NOT NULL, "timestamp" TIMESTAMP DEFAULT CURRENT_TIMESTAMP, animal_id INT NOT NULL, CONSTRAINT fk_animal_images_animal_id FOREIGN KEY (animal_id) REFERENCES animals(id) ON DELETE CASCADE ON UPDATE RESTRICT)