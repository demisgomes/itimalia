CREATE TABLE IF NOT EXISTS users (id SERIAL PRIMARY KEY, email VARCHAR(255) NOT NULL, "password" VARCHAR(255) NOT NULL, birth_date TIMESTAMP NULL, gender VARCHAR(255) NOT NULL, "name" VARCHAR(255) NOT NULL, phone VARCHAR(255) NOT NULL, "role" VARCHAR(255) NOT NULL, creation_date TIMESTAMP NULL, modification_date TIMESTAMP NULL, token VARCHAR(255) NOT NULL)